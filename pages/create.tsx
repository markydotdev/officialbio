import BuildingBlocks from '../components/BuildingBlocks';

// create horizontal scrollable area with the draggable boxes
// potentially use a minimal layout? idk

function create() {
  return <BuildingBlocks></BuildingBlocks>;
}

export default create;
